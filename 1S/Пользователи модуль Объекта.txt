Процедура ПередЗаписью(Отказ)
	ТекущийЛогин = ЭтотОбъект.Наименование;
	ТекущаяСсылка = ЭтотОбъект.Ссылка;
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	Ссылка
	|ИЗ
	|	Справочник.Пользователи
	|ГДЕ
	|	Наименование = &Логин
	|	И Ссылка <> &ТекущаяСсылка";
	Запрос.УстановитьПараметр("Логин", ТекущийЛогин);
	Запрос.УстановитьПараметр("ТекущаяСсылка", ТекущаяСсылка);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Количество() > 0 Тогда
		Отказ = Истина;
		Сообщить("Логин «" + ТекущийЛогин + "» уже используется другим пользователем!" + Символы.ПС +
		"Выберите другой логин.");
	КонецЕсли;
КонецПроцедуры
