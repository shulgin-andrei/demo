Реквизиты
НовыйПароль - строка
ПодтверждениеПароля - строка
Роль - ПеречислениеСсылка.Роль
ТекущийПароль - строка

Команда
СменитьПароль

параметры
ПараметрПользователь

&НаСервере
функция СменитьПарольНаСервере()
	Если ТекущийПароль = Параметры.ПараметрПользователь.Пароль и НовыйПароль = ПодтверждениеПароля Тогда
		ПользовательОбъект = Параметры.ПараметрПользователь.ПолучитьОбъект();
		ПользовательОбъект.Пароль = НовыйПароль;
		ПользовательОбъект.ДатаВхода = ТекущаяДата();
		ПользовательОбъект.Записать();               
		Возврат Истина;
	Иначе
		Сообщить("Пароли не совпадают");
		Возврат Ложь;
	КонецЕсли;
Конецфункции

&НаКлиенте
функция СменитьПароль(Команда)
	Если СменитьПарольНаСервере()=Истина Тогда
		Если Роль=ПредопределенноеЗначение("Перечисление.Роль.Пользователь") Тогда
			Открытьформу("ОбщаяФорма.ФормаПользователя");   
			Сообщить("Вы успешно авторизовались");
			Этаформа.Закрыть();                 
		КонецЕсли;
		Если Роль=ПредопределенноеЗначение("Перечисление.Роль.Администратор") Тогда
			Открытьформу("Справочник.Пользователи.ФормаСписка");   
			Сообщить("Вы успешно авторизовались");
			Этаформа.Закрыть();                 
		КонецЕсли;
	КонецЕсли;
КонецФункции


&НаСервере
Процедура ПриОткрытииНаСервере()
	Роль = Параметры.ПараметрПользователь.Роль;
КонецПроцедуры


&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ПриОткрытииНаСервере();
КонецПроцедуры